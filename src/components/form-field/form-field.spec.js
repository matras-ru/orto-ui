/* eslint-env jest */
import { mount } from '@vue/test-utils';
import CFormField from './form-field';

describe('Field basic', () => {
    it('is rendered', () => {
        const wrapper = mount(CFormField);
    });
});
